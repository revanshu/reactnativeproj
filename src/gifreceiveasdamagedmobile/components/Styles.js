const COLORS = {
  labelColor: '#828282',
  valueColor: '#4a4a4a',
  borderColor: '#efefef',
  whiteColor: '#ffffff',
  loadingBackgroundColor: '#cccccc',
  footerButtonColor: '#017ACD',
  textBlackColor: 'black',
  scanHeaderColor: '#eee',
  checkMarkDisableColor: '#9b9b9b',
  checkMarkSelectedColor: '#4caf50',
};

const FONTS = {
  fontFamily: 'sans-serif',
  fontFamilyLight: 'sans-serif-light',
  fontFamilyMedium: 'sans-serif-medium',
  labelFontSize: 14,
  headerFontSize: 18,
};

const MARGINS = {
  bottom: 5,
  verySmall: 8,
  small: 10,
  average: 16,
  high: 24,
};

const SIZE = {
  iconSize: 30,
  plusMinusIconSize: 20,
};

export {
  COLORS, FONTS, MARGINS, SIZE,
};
